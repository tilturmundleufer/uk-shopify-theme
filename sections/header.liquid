{%- comment -%}
  Header Section
  Inspired by unterkonstruktion.de
  Includes: Topbar with secondary links, Main header with search, Navigation bar
{%- endcomment -%}

{%- liquid
  assign logo = section.settings.logo
  assign logo_width = section.settings.logo_width | default: 120
  assign logo_src_width = logo_width | times: 2
  assign menu = section.settings.menu
  assign show_topbar = section.settings.show_topbar
  assign cta_text = section.settings.cta_text
  assign cta_url = section.settings.cta_url
  assign sticky_header = section.settings.sticky_header
  assign show_account = section.settings.show_account_link
  assign account_label = section.settings.account_label
  assign account_url = section.settings.account_url
  if account_url == blank
    assign account_url = routes.account_login_url
  endif
  assign has_category_bar = false
  for block in section.blocks
    if block.type == 'category_bar_item'
      assign has_category_bar = true
      break
    endif
  endfor
-%}

<header class="header{% if sticky_header %} header--sticky{% endif %}" data-header>
  {%- comment -%} Top Bar with secondary links {%- endcomment -%}
  {%- if show_topbar -%}
    <div class="topbar">
      <div class="page-width">
        <div class="topbar__inner">
          <nav class="topbar__nav" aria-label="{{ 'sections.header.secondary_menu' | t }}">
            {%- for block in section.blocks -%}
              {%- if block.type == 'topbar_link' -%}
                <a href="{{ block.settings.link }}" class="topbar__nav-link">
                  {{ block.settings.label }}
                </a>
              {%- endif -%}
            {%- endfor -%}
          </nav>
        </div>
      </div>
    </div>
  {%- endif -%}
  
  {%- comment -%} Main Header {%- endcomment -%}
  <div class="header__main">
    <div class="page-width">
      <div class="header__inner">
        {%- comment -%} Logo {%- endcomment -%}
        <div class="header__logo">
          <a href="{{ routes.root_url }}" class="header__logo-link">
            {%- if logo != blank -%}
              <img
                src="{{ logo | image_url: width: logo_src_width }}"
                alt="{{ logo.alt | default: shop.name | escape }}"
                class="header__logo-img"
                width="{{ logo_width }}"
                height="{{ logo_width | divided_by: logo.aspect_ratio | round }}"
                loading="eager"
                decoding="async"
              >
            {%- else -%}
              <span class="header__logo-text">{{ shop.name }}</span>
            {%- endif -%}
          </a>
        </div>
        
        {%- comment -%} Search Bar {%- endcomment -%}
        <div class="header__search" data-search-wrapper>
          <form action="{{ routes.search_url }}" method="get" class="header__search-form" role="search">
            <input type="hidden" name="type" value="product">
            <span class="header__search-icon" aria-hidden="true">{%- render 'icon-search' -%}</span>
            <input
              type="search"
              name="q"
              class="header__search-input"
              placeholder="{{ 'sections.header.search_placeholder' | t | default: 'Alle Produkte durchsuchen...' }}"
              aria-label="{{ 'sections.header.search' | t }}"
              autocomplete="off"
              data-search-input
            >
          </form>
          <div class="search-results" data-search-results aria-live="polite" hidden></div>
        </div>
        
        {%- comment -%} Actions {%- endcomment -%}
        <div class="header__actions">
          {%- if show_account and account_label != blank -%}
            <a href="{{ account_url }}" class="header__account-link">
              {%- render 'icon-user', class: 'header__account-icon' -%}
              <span class="header__account-text">{{ account_label }}</span>
            </a>
          {%- endif -%}
          {%- if cta_text != blank -%}
            <a href="{{ cta_url | default: '#' }}" class="btn btn--cta header__cta">
              {{ cta_text }}
            </a>
          {%- endif -%}
          
          {%- comment -%} Cart Button with text (Desktop) {%- endcomment -%}
          <a href="{{ routes.cart_url }}" class="header__cart-btn" aria-label="{{ 'sections.header.cart' | t }}">
            {%- render 'icon-cart' -%}
            <span>{{ 'sections.header.cart_text' | t | default: 'Warenkorb' }}</span>
          </a>
          
          {%- comment -%} Cart Icon (Mobile) {%- endcomment -%}
          <a href="{{ routes.cart_url }}" class="header__action-btn header__cart-icon-mobile" aria-label="{{ 'sections.header.cart' | t }}">
            {%- render 'icon-cart' -%}
            <span class="header__cart-count" data-cart-count>{{ cart.item_count }}</span>
          </a>
          
          <button 
            type="button" 
            class="header__action-btn header__menu-toggle" 
            aria-label="{{ 'accessibility.menu' | t }}"
            aria-expanded="false"
            aria-controls="mobile-menu"
            data-menu-toggle
          >
            {%- render 'icon-menu' -%}
          </button>
        </div>
      </div>
    </div>
  </div>
  
  {%- comment -%} Gelbe Kategorien-Leiste (Blöcke wie Topbar, kein Navigationsmenü) {%- endcomment -%}
  {%- if has_category_bar -%}
    <div class="header__nav-bar">
      <div class="page-width">
        <nav class="header__nav" aria-label="{{ section.settings.category_bar_aria_label | escape }}">
          {%- for block in section.blocks -%}
            {%- if block.type == 'category_bar_item' -%}
              <div class="header__nav-item" {{ block.shopify_attributes }}>
                <a href="{% if block.settings.link != blank %}{{ block.settings.link }}{% else %}#{% endif %}" class="header__nav-link">
                  {%- if block.settings.icon != blank -%}
                    <span class="header__nav-icon-wrap" aria-hidden="true">{{ block.settings.icon }}</span>
                  {%- elsif block.settings.image != blank -%}
                    <img
                      src="{{ block.settings.image | image_url: width: 48 }}"
                      alt=""
                      class="header__nav-icon header__nav-icon--custom"
                      width="24"
                      height="24"
                      loading="eager"
                      decoding="async"
                    >
                  {%- else -%}
                    <svg class="header__nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true" focusable="false"><circle cx="12" cy="12" r="10"/></svg>
                  {%- endif -%}
                  {%- if block.settings.label != blank -%}
                    <span>{{ block.settings.label }}</span>
                  {%- endif -%}
                </a>
              </div>
            {%- endif -%}
          {%- endfor -%}
        </nav>
      </div>
    </div>
  {%- endif -%}
  
  {%- comment -%} Mobile Menu Drawer {%- endcomment -%}
  <div class="mobile-menu" id="mobile-menu" data-mobile-menu aria-hidden="true">
    <div class="mobile-menu__header">
      <div class="header__logo">
        <a href="{{ routes.root_url }}" class="header__logo-link">
          {%- if logo != blank -%}
            <img
              src="{{ logo | image_url: width: logo_src_width }}"
              alt="{{ logo.alt | default: shop.name | escape }}"
              class="header__logo-img"
              width="{{ logo_width }}"
              height="{{ logo_width | divided_by: logo.aspect_ratio | round }}"
              loading="lazy"
              decoding="async"
            >
          {%- else -%}
            <span class="header__logo-text">{{ shop.name }}</span>
          {%- endif -%}
        </a>
      </div>
      <button 
        type="button" 
        class="mobile-menu__close" 
        aria-label="{{ 'accessibility.close' | t }}"
        data-menu-close
      >
        {%- render 'icon-close' -%}
      </button>
    </div>
    
    <div class="mobile-menu__content">
      {%- comment -%} Mobile Search {%- endcomment -%}
      <div class="mobile-menu__search" data-search-wrapper>
        <form action="{{ routes.search_url }}" method="get" class="header__search-form" role="search">
          <input type="hidden" name="type" value="product">
          <span class="header__search-icon" aria-hidden="true">{%- render 'icon-search' -%}</span>
          <input
            type="search"
            name="q"
            class="header__search-input"
            placeholder="{{ 'sections.header.search_placeholder' | t | default: 'Produkte durchsuchen...' }}"
            aria-label="{{ 'sections.header.search' | t }}"
            autocomplete="off"
            data-search-input
          >
        </form>
        <div class="search-results" data-search-results aria-live="polite" hidden></div>
      </div>

      {%- if show_account and account_label != blank -%}
        <a href="{{ account_url }}" class="mobile-menu__account">
          {%- render 'icon-user', class: 'mobile-menu__account-icon' -%}
          {{ account_label }}
        </a>
      {%- endif -%}
      
      <nav class="mobile-menu__nav" aria-label="{{ 'sections.header.menu' | t }}">
        {%- for link in linklists[menu].links -%}
          <div class="mobile-menu__nav-item">
            {%- if link.links != blank -%}
              <button 
                class="mobile-menu__nav-link" 
                aria-expanded="false"
                data-submenu-toggle
              >
                {{ link.title }}
                {%- render 'icon-chevron' -%}
              </button>
              <div class="mobile-menu__subnav" data-submenu hidden>
                {%- for child_link in link.links -%}
                  <a href="{{ child_link.url }}" class="mobile-menu__subnav-link">
                    {{ child_link.title }}
                  </a>
                {%- endfor -%}
              </div>
            {%- else -%}
              <a href="{{ link.url }}" class="mobile-menu__nav-link">
                {{ link.title }}
              </a>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </nav>
      
      <div class="mobile-menu__contact">
        {%- if settings.contact_phone != blank -%}
          <a href="tel:{{ settings.contact_phone | remove: ' ' }}" class="mobile-menu__contact-link">
            {%- render 'icon-phone' -%}
            <span>{{ settings.contact_phone }}</span>
          </a>
        {%- endif -%}
        {%- if settings.contact_email != blank -%}
          <a href="mailto:{{ settings.contact_email }}" class="mobile-menu__contact-link">
            {%- render 'icon-mail' -%}
            <span>{{ settings.contact_email }}</span>
          </a>
        {%- endif -%}
      </div>
      
      {%- if cta_text != blank -%}
        <a href="{{ cta_url | default: '#' }}" class="btn btn--cta btn--full mobile-menu__cta">
          {{ cta_text }}
        </a>
      {%- endif -%}
    </div>
  </div>
  
  <div class="header-overlay" data-header-overlay></div>
</header>

{% schema %}
{
  "name": "Header",
  "class": "section-header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo"
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "Logo-Breite",
      "min": 50,
      "max": 250,
      "step": 10,
      "default": 120,
      "unit": "px"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Hauptmenü (Mobil & Suche)",
      "default": "main-menu",
      "info": "Wird im mobilen Menü und für die Suche genutzt."
    },
    {
      "type": "text",
      "id": "category_bar_aria_label",
      "label": "Gelbe Leiste (Barrierefreiheit)",
      "default": "Produktkategorien",
      "info": "Kurze Beschreibung für Screenreader (verstecktes nav-Label)."
    },
    {
      "type": "checkbox",
      "id": "show_topbar",
      "label": "Topbar anzeigen",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sticky_header",
      "label": "Sticky Header",
      "default": true
    },
    {
      "type": "header",
      "content": "Kundenkonto"
    },
    {
      "type": "checkbox",
      "id": "show_account_link",
      "label": "Link „Mein Konto“ anzeigen",
      "default": true
    },
    {
      "type": "text",
      "id": "account_label",
      "label": "Konto-Text",
      "default": "Mein Konto"
    },
    {
      "type": "url",
      "id": "account_url",
      "label": "Konto-Link",
      "info": "Leer lassen für Shopify Login (/account/login)"
    },
    {
      "type": "header",
      "content": "CTA Button"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "Button-Text",
      "default": "Zum Konfigurator"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Button-Link"
    }
  ],
  "blocks": [
    {
      "type": "topbar_link",
      "name": "Topbar Link",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Text",
          "default": "Kontakt"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    },
    {
      "type": "category_bar_item",
      "name": "Gelbe Leiste – Punkt",
      "settings": [
        {
          "type": "paragraph",
          "content": "Reihenfolge wie bei Topbar-Links: Blöcke im Theme-Editor sortieren. Zuerst SVG-Code, sonst Bild; ohne beides erscheint ein Platzhalter-Kreis."
        },
        {
          "type": "text",
          "id": "label",
          "label": "Titel",
          "default": "Kategorie"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link",
          "info": "Zielseite oder Collection. Leer = #."
        },
        {
          "type": "html",
          "id": "icon",
          "label": "Icon (SVG-Code)",
          "info": "Vollständiges <svg>…</svg> (z. B. viewBox 0 0 24 24). Wird vor „Icon (Bild)“ verwendet."
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Icon (Bild)",
          "info": "PNG oder hochgeladene SVG-Datei, wenn kein SVG-Code eingetragen ist."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Header",
      "blocks": [
        { "type": "topbar_link", "settings": { "label": "Kontakt" } },
        { "type": "topbar_link", "settings": { "label": "Über Uns" } },
        { "type": "topbar_link", "settings": { "label": "FAQ" } },
        { "type": "category_bar_item", "settings": { "label": "Unterkonstruktion" } },
        { "type": "category_bar_item", "settings": { "label": "Solarmodule" } },
        { "type": "category_bar_item", "settings": { "label": "Wechselrichter" } },
        { "type": "category_bar_item", "settings": { "label": "Batteriespeicher" } },
        { "type": "category_bar_item", "settings": { "label": "Zubehör" } }
      ]
    }
  ]
}
{% endschema %}
